# WHCA Dinner 2026 Seating Graphic

Interactive seating chart showing where top officials were seated at the White House Correspondents' Association Dinner 2026.

## Dev

Open `index.html` directly in a browser — no build step needed.

## Data

Seating and guest data lives in `data/`. Python notebooks in `notebooks/` were used for data processing (`uv run jupyter lab`).
