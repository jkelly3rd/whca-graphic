name: Publish Analysis

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Install uv
      uses: astral-sh/setup-uv@v4

    - name: Set up Python
      run: uv python install 3.13

    - name: Install dependencies
      run: uv sync

    - name: Run notebooks
      run: |
        uv run jupyter nbconvert --to notebook --execute --allow-errors --inplace notebooks/[0-9]*.ipynb
        uv run jupyter nbconvert --to html notebooks/[0-9]*.ipynb

    - name: Upload analysis artifacts
      uses: actions/upload-artifact@v4
      with:
        name: analysis-results
        path: |
          notebooks/*.html
          data/processed/
